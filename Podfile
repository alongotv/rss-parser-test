# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'rss-parser-test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'FeedKit', '~> 8.0'

  # Pods for rss-parser-test

  target 'rss-parser-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'rss-parser-testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
